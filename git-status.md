Definition: git status
Git Status command would display, in the terminal window, the state of the working directory and the staging area. It shows whether changes have been staged or what files are untracked by Git. 
To do gain the same information from JupyterLab IDE, click the Git button of the left side, then click "Changes" found in the middle of the newly popped screen.

